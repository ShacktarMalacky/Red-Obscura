/** @type {import('next').NextConfig} */
const nextConfig = {
  // Si usas el App Router, no necesitas configurar appDir aquí.
  // Next.js 16 lo detecta automáticamente.
};

module.exports = nextConfig;
